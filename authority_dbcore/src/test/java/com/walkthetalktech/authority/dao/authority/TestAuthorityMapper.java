package com.walkthetalktech.authority.dao.authority;

import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.walkthetalktech.authority.model.authority.Authority;

/*@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations={"classpath:authority/spring/spring-mybatis.xml"})*/
public class TestAuthorityMapper {

	/*@Autowired
	private IAuthorityMapper authorityMapper;*/
	
	
	@Test
	public void testAuthorityMapper(){
		/*Authority param=new Authority();
		param.setUserId(1l);
		List<Authority> authorityList=authorityMapper.selectAuthorityListByAuthority(param);
		System.out.println(authorityList);*/
	}
}
